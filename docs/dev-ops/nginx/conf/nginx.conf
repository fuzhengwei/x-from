server {
    listen 80;
    server_name localhost;

    # 管理端（首页 + JS + 静态资源）
    location /admin/ {
        alias /usr/share/nginx/html/admin/;
        index index.html;
        try_files $uri $uri/ /admin/index.html;
    }

    # 用户端 - 表单填写
    location /user/form.html {
        alias /usr/share/nginx/html/user/form.html;
    }

    # 用户端 - 智能客服
    location /user/chat.html {
        alias /usr/share/nginx/html/user/chat.html;
    }

    # 用户端静态资源
    location /user/ {
        alias /usr/share/nginx/html/user/;
        try_files $uri $uri/ /user/form.html;
    }

    # API 代理 → 后端 Spring Boot
    location /api/ {
        proxy_pass http://x_from_app:8091/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 聊天接口可能较慢，增加超时
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;

        # CORS
        add_header Access-Control-Allow-Origin * always;
        add_header Access-Control-Allow-Methods 'GET, POST, PUT, DELETE, OPTIONS' always;
        add_header Access-Control-Allow-Headers 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization' always;

        if ($request_method = 'OPTIONS') {
            return 204;
        }
    }

    # 默认重定向到管理端
    location = / {
        return 302 /admin/;
    }
}
